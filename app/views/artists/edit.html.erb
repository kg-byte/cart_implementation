<%= render partial: "form", locals: {
                                    method: :patch,
                                    path: "/artists/#{@artist.id}",
                                    submit_text: "Update Artist"
                                        } %>
